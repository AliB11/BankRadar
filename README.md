# بانک‌رادار · رادار محصولات بانکی ایران

سامانه مقایسه، رتبه‌بندی و پایش محصولات بانکی ایران — سپرده‌ها، اعتبار، تسهیلات و باشگاه‌های امتیازی — با
امتیازدهی شفاف از دید مشتری و **تعدیل‌شده نسبت به تورم**.

> نکته کلیدی این پروژه: در اقتصادی با تورم بالای ۶۰ درصد، عدد اسمی نرخ سود به‌تنهایی گمراه‌کننده است.
> سپرده یک‌ساله با سود ۲۳ درصد بازده حقیقی عمیقاً منفی دارد و وام قرض‌الحسنه با کارمزد ۴ درصد ارزان‌ترین
> منبع تأمین مالی تاریخ اخیر است. این سامانه هر دو را با یک معیار قابل‌مقایسه می‌سنجد.

---

## فهرست

- [قابلیت‌ها](#قابلیتها)
- [شروع سریع](#شروع-سریع)
- [معماری](#معماری)
- [مدل داده](#مدل-داده)
- [خط لوله گردآوری داده](#خط-لوله-گردآوری-داده)
- [به‌روزرسانی خودکار با GitHub Actions](#بهروزرسانی-خودکار-با-github-actions)
- [روش امتیازدهی](#روش-امتیازدهی)
- [منابع داده](#منابع-داده)
- [توسعه و آزمون](#توسعه-و-آزمون)

---

## قابلیت‌ها

**مقایسه و تحلیل**
- بیش از ۲۰۰ محصول مالی از ده‌ها بانک، مؤسسه اعتباری و نهاد مالی در پنج دسته: منابعی و سپرده، صندوق‌های درآمد ثابت، اعتباری، تسهیلاتی، امتیازی
  (شمار دقیق و زنده در خود سامانه نمایش داده می‌شود و با هر واکشی عوض می‌شود)
- **بخش مجزا برای صندوق‌های سرمایه‌گذاری با درآمد ثابت (Fixed-Income Funds)** با رتبه‌بندی جذابیت اختصاصی، سود مؤثر سالانه تا ۳۷٪، تفکیک ETF و صدور/ابطالی و روزشمار
- **پایش و رصد سودهای ترجیحی بانک‌ها**: نردبان کامل نرخ‌های توافقی (۲۲ تا ۳۱ درصد) به تفکیک مانده برای سپرده‌های کلان، به‌همراه طرح‌های ویژه خارج از سقف متعارف
- امتیاز «جذابیت» ۰ تا ۱۰۰ با پنج بعد وزن‌دهی‌شدنی و پنج پروفایل آماده
- **بازده حقیقی** بر پایه رابطه فیشر: نرخ اسمی در برابر تورم، با نمایش صریح فرسایش قدرت خرید
- **ماشین‌حساب اقساط** با هر دو روش رایج بانکی (اقساط مساوی و سود مازاد) و محاسبه نرخ مؤثر
- جدول مقایسه کنار‌به‌کنار با برجسته‌سازی مقادیر برتر هر معیار
- **فیلتر نوع عقد** در تسهیلات و اعتبار: تفکیک قرض‌الحسنه، عقود غیرمشارکتی (مبادله‌ای) و مشارکتی
- نمودار نردبان نرخ‌ها در برابر خط تورم و نقشه حرارتی تازگی داده

**کیفیت داده**
- هر رکورد دارای منبع، نوع منبع، سطح اطمینان و تاریخ کنترل است
- رکوردهایی که در واکشی دیده نشوند **حذف نمی‌شوند**؛ با نشان «نیازمند بازبینی» علامت می‌خورند
- اعتبارسنجی طرح‌واره در CI؛ در صورت خطا، انتشار متوقف می‌شود
- خروجی JSON و CSV برای استفاده در تحلیل‌های دیگر

**مهندسی**
- بدون وابستگی خارجی در زمان اجرا (نه فریم‌ورک، نه کتابخانه نمودار)
- بدون سرور؛ روی GitHub Pages یا حتی با باز کردن مستقیم فایل کار می‌کند
- آزمون‌های واحد برای تبدیل تقویم شمسی، تجزیه متن فارسی، ادغام داده، محاسبات مالی و امنیت قالب‌ها

---

## شروع سریع

نیازمندی: Node.js نسخه ۲۰ یا بالاتر. هیچ وابستگی خارجی نصب نمی‌شود.

```bash
git clone <repo-url> && cd <repo>

npm run serve          # پیش‌نمایش روی http://localhost:4173
npm test               # اعتبارسنجی داده + اجرای همه آزمون‌ها
npm run collect        # واکشی تازه از منابع و به‌روزرسانی داده
npm run collect:dry    # واکشی آزمایشی بدون نوشتن
npm run collect:offline # بازسازی بسته داده بدون شبکه
```

باز کردن مستقیم `index.html` هم کار می‌کند: داده از `data/bundle.js` بارگذاری می‌شود تا
محدودیت CORS در محیط `file://` مشکلی ایجاد نکند.

---

## معماری

```
├── index.html                  پوسته سامانه (فقط ساختار، بدون منطق)
├── assets/
│   ├── css/app.css             سیستم طراحی: توکن، اجزا، چیدمان، چاپ
│   └── js/
│       ├── app.js              راه‌انداز، رویدادها، ماشین‌حساب، خروجی
│       ├── store.js            وضعیت مرکزی، بارگذاری داده، فیلتر، مرتب‌سازی
│       ├── views.js            تولید HTML همه بخش‌ها
│       ├── score.js            موتور امتیازدهی و اصلاحات واقع‌گرایانه
│       ├── finance.js          محاسبات اقساط، نرخ مؤثر، بازده حقیقی
│       ├── charts.js           نمودارهای SVG بدون کتابخانه
│       └── util.js             قالب‌بندی فارسی، امنیت، تقویم، ذخیره‌سازی
├── data/
│   ├── products.json           منبع حقیقت محصولات
│   ├── banks.json              فهرست بانک‌ها و مؤسسات مجاز
│   ├── indicators.json         شاخص‌های کلان و نرخ‌های مرجع
│   ├── schema.json             طرح‌واره اعتبارسنجی
│   ├── meta.json               گزارش آخرین اجرای خط لوله
│   └── bundle.js               بسته درون‌خطی (ساخته‌شده)
├── tools/
│   ├── collect.mjs             هماهنگ‌کننده خط لوله
│   ├── build-bundle.mjs        ساخت بسته داده
│   ├── data-signature.mjs      امضای محتوایی داده (دروازه «تغییر واقعی»)
│   ├── serve.mjs               سرور ایستا برای توسعه
│   ├── lib/{http,parse,jalali,sitemap}.mjs
│   └── sources/{rade,banks,cbi,dgshahr}.mjs
├── scripts/validate-data.mjs   دروازه کیفیت داده
├── tests/                      آزمون‌های واحد، رگرسیون و یکپارچگی
└── .github/workflows/          به‌روزرسانی، انتشار، CI
```

### چرا بدون فریم‌ورک؟

این پروژه یک سامانه داده‌محور با تعامل محدود است. استفاده از فریم‌ورک، حجم بسته را چند برابر می‌کرد
بی‌آنکه قابلیتی اضافه شود. ماژول‌های ES بومی مرورگر، تقسیم مسئولیت تمیزی فراهم می‌کنند و
نتیجه در هر مرورگری بدون مرحله ساخت اجرا می‌شود.

### قاعده RTL در CSS (درس یک اشکال واقعی)

ویژگی‌های منطقی مثل `inset-inline-start` با جهت سند عوض می‌شوند، اما `translateX` **فیزیکی**
است و همیشه منفی یعنی چپ. کشوی جزئیات به لبه شروع چسبیده (در RTL یعنی لبه راست) و حالت
بسته‌اش با `translateX(-102%)` نوشته شده بود؛ نتیجه آنکه کشو به‌جای رفتن به بیرون، به وسط
صفحه می‌آمد و یک قاب تمام‌قد روی کل محتوا می‌نشست.

قاعده‌ای که حالا رعایت می‌شود و `tests/css.test.mjs` آن را قفل کرده است:

1. جهت جابه‌جایی عنصرِ بیرون‌صفحه باید با جهت سند هم‌خوان شود
   (`[dir='rtl'] .drawer { transform: translateX(102%) }`).
2. لایه‌های ثابتِ تمام‌صفحه در حالت بسته هم `visibility: hidden` و هم `pointer-events: none`
   می‌گیرند تا حتی در صورت خطای هندسی، چیزی روی صفحه نماند.
3. آزمون، هندسه جعبه را برای عرض‌های ۳۶۰ تا ۱۹۲۰ پیکسل بازسازی می‌کند و اگر لایه‌ای
   در حالت پیش‌فرض روی صفحه دیده شود، CI سرخ می‌شود.

---

## مدل داده

هر محصول یک رکورد با این ساختار است (نمای کامل در `data/schema.json`):

```jsonc
{
  "id": "ezdevaj-300",                    // شناسه یکتا، kebab-case
  "bank": "شبکه بانکی (ثبت‌نام: بانک مرکزی)",
  "bankId": "cbi",
  "product": "وام قرض‌الحسنه ازدواج — سقف پایه هر نفر",
  "category": "loans",                    // deposits | funds | credit | loans | loyalty
  "subcategory": "qarz",

  "rate": 4,                              // نرخ یا کارمزد سالانه (درصد)
  "rateKind": "fee",                      // profit | fee | yield | none
  "contractType": "qarz",                 // qarz | non-partnership | partnership | unknown (اختیاری)
  "benefit": 95,                          // مزیت مالی ۰ تا ۱۰۰

  "minAmount": 300000000,
  "maxAmount": 300000000,
  "amountLabel": "۳۰۰ میلیون تومان برای هر یک از زوجین",
  "termMonths": 120,
  "termLabel": "۱۲۰ ماه (۱۰ سال)",

  "speed": 38,                            // سرعت دسترسی
  "digital": 66,                          // سهولت دیجیتال
  "friction": 48,                         // کمبود اصطکاک (بالاتر = ساده‌تر)

  "collateral": "ضامن معتبر یا اعتبارسنجی + سفته",
  "collateralKind": "guarantor",          // none | credit-score | guarantor | deposit-block | collateral | mixed
  "audience": "زوجین متقاضی",
  "desc": "…",
  "tags": ["قرض‌الحسنه", "کارمزد ۴٪"],
  "requirements": ["سند ازدواج", "ثبت درخواست در سامانه ve.cbi.ir"],

  "confidence": "high",                   // high | medium | low
  "regulatory": true,                     // مصوب نهاد ناظر
  "lastUpdated": "2026-09-14",

  "source": {
    "title": "نبض قیمت — وام ازدواج ۱۴۰۵",
    "url": "https://nabzgheymat.ir/...",
    "kind": "press",                      // regulator | bank | aggregator | press
    "checked": "2026-09-15"
  }
}
```

رکوردهای گردآوری خودکار این فیلدها را هم دارند: `autoDiscovered: true`، `lastSeen`، و در صورت
قدیمی شدن `stale: true`.

---

## خط لوله گردآوری داده

`tools/collect.mjs` چهار منبع را موازی و مقاوم به خطا واکشی می‌کند:

| منبع | فایل | روش | خروجی |
| --- | --- | --- | --- |
| **رده (rade.ir)** | `tools/sources/rade.mjs` | کشف از `loan-sitemap.xml`، استخراج جدول مشخصات بر پایه **برچسب** نه سلکتور CSS | محصولات خودکار با `rade-<id>` |
| **سایت بانک‌ها** | `tools/sources/banks.mjs` | پایش ۲۰ هدف (بانک مرکزی + ۱۹ بانک)، استخراج نرخ و شرط با الگوی متنی | مشاهدات نرخ برای تأیید |
| **شاخص‌های کلان** | `tools/sources/cbi.mjs` | تورم، نرخ بین‌بانکی و نرخ‌های مرجع با میانه‌گیری برای مقاومت در برابر مقدار پرت | `indicators.json` |
| **دیجی‌شهر (مجله)** | `tools/sources/dgshahr.mjs` | جدول‌های نرخ سود سپرده بانک‌ها: نرخ‌های ترجیحی پلکانی + طرح‌های ویژه؛ طبقه‌بندی سلول بر پایه محتوا (ستون‌های جابه‌جایی‌یافته) و باز کردن rowspan | محصولات `deposits/preferential` و طرح‌های ویژه |

### اصول طراحی خط لوله

۱. **هیچ داده‌ای حذف نمی‌شود.** رکورد دست‌نویس هرگز با داده خودکار بازنویسی نمی‌شود؛ فیلدهای
   ساختاری (دسته، امتیازها، منبع، سطح اطمینان) قفل‌اند. رکورد خودکارِ دیده‌نشده فقط `stale` می‌شود.

۲. **شکست یک منبع، اجرا را متوقف نمی‌کند.** هر منبع در `runSource` پیچیده شده و خطا در
   `meta.json` ثبت می‌شود؛ بقیه منابع به کار خود ادامه می‌دهند.

۳. **تطبیق دو‌مرحله‌ای.** تطبیق ابتدا با `id` و در صورت نبود، با کلید `bank::product` انجام می‌شود
   تا تغییر شناسه در منبع، رکورد تکراری نسازد.

۴. **خروجی پایدار.** کلیدهای JSON بازگشتی مرتب می‌شوند تا `git diff` فقط تغییرات معنادار را نشان دهد.

۵. **استخراج بر پایه برچسب.** صفحات رده جدول مشخصاتی با برچسب‌های ثابت دارند
   («نرخ سود وام»، «سقف وام»، «نوع ضمانت»). این روش در برابر تغییر قالب مقاوم است،
   برخلاف سلکتور CSS که با هر بازطراحی می‌شکند.

### اجرای خط لوله

```bash
node tools/collect.mjs                  # واکشی کامل
node tools/collect.mjs --limit=40       # محدودکردن تعداد صفحات
node tools/collect.mjs --dry-run        # بدون نوشتن
node tools/collect.mjs --offline        # بدون شبکه
node tools/collect.mjs --verbose        # گزارش کامل JSON
node tools/data-signature.mjs --json    # امضای محتوایی داده + شمارش‌ها
```

### امضای داده: تفکیک «تغییر واقعی» از «مهر زمانی»

`tools/data-signature.mjs` اثر انگشت محتوای داده را می‌سازد: محصولات، شاخص‌ها، بازه‌های
مصوب و فهرست بانک‌ها — **بدون** فیلدهای فرار (`generatedAt`، `lastRun`، مدت واکشی).

چرا لازم است: `products.json` در هر اجرا مهر زمانی تازه می‌گیرد، پس `git diff` همیشه
پر است. اگر ملاک کامیت همان diff باشد، هر اجرا — حتی وقتی هیچ منبعی پاسخ نداده —
یک کامیت بی‌محتوا و یک انتشار بیهوده می‌سازد. با امضا، تنها وقتی کامیت ساخته می‌شود
که واقعاً چیزی عوض شده باشد.

`lastSeen` و `lastVerified` عمداً بخشی از امضا هستند: تغییرشان یعنی خط لوله رکوردی را
بازبینی کرده، که یک به‌روزرسانی معنادار است.

---

## به‌روزرسانی خودکار با GitHub Actions

### `.github/workflows/refresh-data.yml`

| زمان‌بندی | توضیح |
| --- | --- |
| `30 3 * * *` | هر روز، ۰۷:۰۰ به وقت تهران — پیش از شروع روز کاری بانک‌ها |
| `0 5 * * 4` | هر پنجشنبه — بازبینی هفتگی جامع‌تر |
| دستی | از تب Actions با امکان تعیین محدودیت و حالت آزمایشی |

مراحل اجرا:

```
دریافت مخزن → Node 22 → امضای داده پیش از اجرا → واکشی داده → اعتبارسنجی
   → آزمون‌ها → بازسازی بسته → مقایسه امضا → ثبت و push (فقط اگر محتوا عوض شده باشد)
   → گزارش تشخیصی → خلاصه اجرا → (در شکست) دم لاگ + ایشو اطلاع‌رسانی
```

نکات پیاده‌سازی مهم:

- **پوسته bash با pipefail**: خروجی واکشی با `| tee collect.log` هم به لاگ می‌رود هم به
  کنسول. در پوسته پیش‌فرض Actions (بدون pipefail) کد خروج `tee` ملاک است، یعنی شکست سخت
  `collect.mjs` بی‌صدا رد می‌شد و اجرا «موفق» گزارش می‌کرد. با `defaults.run.shell: bash`
  این حفره بسته شده است.
- **ثبت شرطی بر پایه امضا**: اگر امضای محتوایی داده تغییر نکند، هیچ کامیتی ساخته نمی‌شود
  و انتشار بیهوده‌ای رخ نمی‌دهد (فقط مهرهای زمانی عوض شده‌اند).
- **همگام‌سازی پیش از push**: `git pull --rebase` پیش از `git push origin HEAD:<branch>`؛
  اگر بین واکشی و ثبت، کامیت دیگری روی شاخه نشسته باشد، به‌روزرسانی آن روز رد نمی‌شود.
- **دروازه کیفیت**: اگر `validate-data.mjs` یا آزمون‌ها شکست بخورند، تغییرات هرگز commit
  نمی‌شوند؛ سامانه با آخرین داده معتبر به کار ادامه می‌دهد.
- **اطلاع‌رسانی شکست**: در صورت خطا، ایشویی با برچسب `data-pipeline` ثبت (یا به ایشو باز
  موجود اضافه) می‌شود. برچسب‌ها پیش از استفاده **در خود جریان کاری ساخته می‌شوند**، چون
  ایشو با برچسب ناموجود خطای 422 می‌گیرد و همان اطلاع‌رسانی از دست می‌رود.
- **خلاصه اجرا**: سلامت هر منبع، امضای پیش/پس و وضعیت کامیت در `GITHUB_STEP_SUMMARY`
  درج می‌شود و در شکست، ۴۰ خط آخر لاگ هم بدون نیاز به بارگیری artifact دیده می‌شود.

### `.github/workflows/deploy-pages.yml`

پس از هر push روی `main` و پس از هر اجرای موفق به‌روزرسانی داده، بسته انتشار را می‌سازد
(فقط `index.html`، `assets/` و `data/`) و روی GitHub Pages منتشر می‌کند.

برای فعال‌سازی: در تنظیمات مخزن، **Settings → Pages → Source** را روی **GitHub Actions** بگذارید.

### `.github/workflows/ci.yml`

روی هر push و pull request: اعتبارسنجی داده، ۱۱۴ آزمون واحد، بررسی هم‌خوانی `bundle.js` با
`products.json`، و بررسی صحت نحو همه فایل‌های جاوااسکریپت.

---

## روش امتیازدهی

امتیاز جذابیت از میانگین وزنی پنج بعد ساخته می‌شود:

| بعد | بازه پیش‌فرض | معنا |
| --- | --- | --- |
| مزیت مالی | ۰ – ۵۰ | نرخ، بازده و هزینه مالی نسبت به گزینه‌های جایگزین |
| سهولت دیجیتال | ۰ – ۴۰ | امکان انجام کامل فرآیند بدون مراجعه حضوری |
| سرعت دسترسی | ۰ – ۳۰ | زمان از درخواست تا دریافت |
| کمبود وثیقه | ۰ – ۳۰ | نبود ضامن و وثیقه و سپرده مسدودی |
| تازگی داده | ۰ – ۲۰ | کنترل‌شدن اطلاعات در دوره اخیر |

سپس سه اصلاح واقع‌گرایانه اعمال می‌شود:

**۱. نرخ حقیقی.** برای محصولات منابعی، فاصله نرخ اسمی از تورم به‌صورت جریمه وارد می‌شود
(حداکثر ۱۲ نمره). برای تسهیلات قرض‌الحسنه، پاداش مثبت (حداکثر ۱۰ نمره) اعمال می‌شود،
چون بازده حقیقی آن برای وام‌گیرنده چشمگیر است.

**۲. هزینه فرصت.** تسهیلات سپرده‌محور ۵ نمره جریمه می‌گیرند: پولی که برای امتیازسازی خواب
می‌ماند، از بازده بازار محروم می‌شود و تورم آن را می‌فرساید.

**۳. اعتبار منبع.** رکوردهای خودکار، کم‌اطمینان یا نیازمند بازبینی حداکثر ۸ درصد امتیاز
کمتری می‌گیرند.

پروفایل‌های آماده: متعادل، کم‌هزینه‌ترین، دیجیتال‌محور، سریع‌ترین، بدون وثیقه.

### محاسبات مالی

سه شیوه هزینه‌گذاری در نظام بانکی ایران رایج است و نادیده گرفتن تفاوت آن‌ها نتیجه را
به‌شدت گمراه‌کننده می‌کند. سامانه هر سه را می‌شناسد و شیوه را از خود محصول (`rateKind`)
می‌خواند، نه از عدد نرخ:

```
سود سالانه (profit)
    روش جدید (اقساط مساوی):  قسط = P·r ÷ (1 − (1+r)^−n)
    روش قدیمی (سود مازاد):   سود = P·R·(n+1) ÷ 2400 ،  قسط = (P + سود) ÷ n

کارمزد یک‌بار (fee) — قرض‌الحسنه و وام‌های حمایتی
    هزینه = P × کارمزد٪ ،  قسط = (P + هزینه) ÷ n

نرخ حقیقی:  (1 + i) ÷ (1 + π) − 1
نرخ مؤثر:   نرخ تنزیلی که ارزش فعلی اقساط را با خالص دریافتی برابر می‌کند (IRR)
```

که `P` اصل، `R` نرخ سالانه، `r` نرخ ماهانه و `n` تعداد اقساط است.

**چرا تفکیک `fee` مهم است؟** در وام قرض‌الحسنه، عدد ۴٪ نرخ سالانه نیست؛ کارمزدی است که یک بار
روی کل اصل بسته می‌شود. اگر آن را نرخ سالانه فرض کنیم، هزینه یک وام ازدواج ۳۰۰ میلیونی ده‌ساله
حدود **ده برابر واقعیت** برآورد می‌شود (۴۰٪ اصل به‌جای ۴٪). این سامانه برای وام ازدواج ۳۰۰ میلیونی
با کارمزد ۴٪ محاسبه می‌کند:

| | مقدار |
| --- | --- |
| قسط ماهانه (۱۲۰ ماه) | ۲٬۶۰۰٬۰۰۰ تومان |
| کل کارمزد | ۱۲٬۰۰۰٬۰۰۰ تومان |
| نرخ مؤثر سالانه | ۰٫۷۸٪ |
| هزینه حقیقی در تورم ۶۹٫۹٪ | −۴۰٫۷٪ |

### یادداشت صحت‌سنجی

برای وام ۱۰۰ میلیون تومانی با نرخ ۱۸٪ و ۴۸ قسط، قسط دقیق روش جدید **۲٬۹۳۷٬۵۰۰** تومان است
(با شبیه‌سازی جدول استهلاک تأیید شده: مانده در پایان دوره صفر می‌شود). برخی گزارش‌های
رسانه‌ای عدد ۲٬۹۳۸٬۸۶۳ را ذکر می‌کنند که با محاسبه دقیق هم‌خوان نیست؛ مرجع این پروژه
محاسبه ریاضی است و آزمون‌ها آن را قفل کرده‌اند.

نرخ مؤثری که این سامانه گزارش می‌کند، **کف** هزینه است: هزینه‌های جانبی مانند بیمه،
کارمزد ضامن و هزینه فرصت سپرده را در بر نمی‌گیرد. ارقام بالاتر (۳۳ تا ۳۵ درصد) که در
گزارش‌های میدانی برای تسهیلات ۲۳ درصدی نقل می‌شود، مجموع همه این هزینه‌هاست.

### سقف مشروط

سقف برخی محصولات یک حق قطعی نیست و به سپرده، امتیاز یا مصوبه اعتباری وابسته است (مثلاً
«چند برابر میانگین حساب»). این رکوردها با نشان `ceilingContingent` علامت می‌خورند و در
جدول مقایسه با نماد «≈» نمایش داده می‌شوند و **در انتخاب مقدار برتر هر ردیف شرکت نمی‌کنند**؛
در غیر این صورت محصولی با سقف چند میلیاردیِ فرضی، برنده «کم‌ترین قسط» می‌شد و مقایسه
بی‌معنا می‌گشت.

---

## منابع داده

| منبع | نوع | کاربرد |
| --- | --- | --- |
| [بانک مرکزی جمهوری اسلامی ایران](https://www.cbi.ir) | نهاد ناظر | نرخ‌های مصوب، بخشنامه‌ها، سامانه‌های ve.cbi.ir |
| [رده — rade.ir](https://www.rade.ir/loan/) | مرجع مقایسه‌ای | ساختار ماشین‌خوان محصولات وام (منبع اصلی خط لوله) |
| [دیجی‌شهر — مقایسه نرخ سود سپرده بانکی](https://dgshahr.com/blog/best-banks-for-deposit-rates/) | مطبوعات / مقایسه‌ای | جدول‌های نرخ‌های ترجیحی پلکانی و طرح‌های ویژه سپرده به تفکیک بانک |
| [مرکز آمار ایران](https://www.amar.org.ir) | نهاد رسمی | شاخص قیمت مصرف‌کننده و تورم |
| سایت رسمی هر بانک | بانک | شرایط و نرخ‌های اختصاصی |
| رسانه‌های اقتصادی | مطبوعات | تأیید متقابل ارقام و شرایط جاری |

نرخ‌ها تابع مصوبات شورای پول و اعتبار و بخشنامه‌های بانک مرکزی است و بانک‌ها می‌توانند
شرایط را تغییر دهند. **پیش از هر اقدام مالی، شرایط را از منبع رسمی بانک تأیید کنید.**

---

## توسعه و آزمون

```bash
npm test                        # اعتبارسنجی + ۱۱۴ آزمون
node --test "tests/*.test.mjs"  # فقط آزمون‌ها
node scripts/validate-data.mjs  # فقط اعتبارسنجی داده
```

پوشش آزمون‌ها:

- `tests/jalali.test.mjs` — تبدیل تقویم شمسی: نوروز ۱۲ سال مرجع، تبدیل رفت‌وبرگشت ۱۰۰۰ روز، کبیسه‌ها
- `tests/parse.test.mjs` — تجزیه ارقام و مبالغ فارسی، نیم‌فاصله، نرخ‌ها، شناسه‌سازی
- `tests/http.test.mjs` — لایه شبکه روی سرور محلی واقعی: retry، خطاهای گذرا/دائمی، کنترل هم‌زمانی
- `tests/merge.test.mjs` — منطق ادغام، حفظ رکورد دست‌نویس، علامت‌گذاری `stale`، استخراج جدول
- `tests/sources.test.mjs` — منبع رده و بانک‌ها روی سرور محلی؛ قرارداد «پاکت نتایج» و رسیدن
  خروجی منبع تا مرحله ادغام (نگهبانِ به‌روزرسانی بی‌صدا)
- `tests/dgshahr.test.mjs` — منبع دیجی‌شهر: تجزیه جدول‌های نرخ (ستون‌های جابه‌جا، rowspan، بازه‌های مبلغی) و نگاشت به محصولات سپرده
- `tests/signature.test.mjs` — امضای داده: قطعی بودن، بی‌حسی به مهرهای زمانی، حساسیت به هر تغییر محتوا
- `tests/render.test.mjs` — تولید HTML همه بخش‌ها، **آزمون‌های امنیتی تزریق**، صحت محاسبات مالی
- `tests/app.test.mjs` — قرارداد رویدادها (هر `data-action` باید مدیریت شود) و راه‌اندازی واقعی سامانه روی DOM شبیه‌سازی‌شده
- `tests/css.test.mjs` — نگهبان چیدمان: هندسه کشوی بسته در RTL/LTR، پنهان بودن همه لایه‌های
  ثابتِ تمام‌صفحه در حالت پیش‌فرض، و نگرفتن کلیک کاربر
- `tests/workflows.test.mjs` — نحو bash همه بلوک‌های `run:`، فعال بودن pipefail در بلوک‌های دارای لوله،
  دام‌های `set -e`، و نبود عبارت‌های قابل‌تزریق در پوسته

### نکته امنیتی

`safeUrl()` تنها طرح‌های `http`، `https` و `mailto` را می‌پذیرد. این تابع لازم است چون
فرار دادن کاراکترهای HTML (`esc`) طرح نشانی را بررسی نمی‌کند و `javascript:` از آن عبور می‌کند.
آزمون‌های `tests/render.test.mjs` این محافظت را قفل کرده‌اند.

---

## مجوز

کد تحت مجوز MIT. داده‌ها از منابع عمومی گردآوری شده و مالکیت آن‌ها با منابع اصلی است.
این سامانه ابزار مقایسه و اطلاع‌رسانی است و توصیه سرمایه‌گذاری یا اعتباری محسوب نمی‌شود.
